// The following operators used to setup conditional logic in our javascript applications

// === - equality operator
// !== - not equal operator
// < - less than operator
// > - greater than operator
// <= - less than or equal operator
// >= - greater than or equal operator

let temp = 8;

if (temp <= 32) {
  console.log("It is freezing outside!");
}

if (temp >= 100) {
  console.log("It is way to hot outside");
}

// challenge

let age = 5;

if (age <= 7) {
  console.log("Chiled discount");
}

if (age >= 65) {
  console.log("Senior discount");
}

// let isChiled = age <= 7;

// let isSenior = age >= 65;

// console.log(`
// age = ${age}
// isChiled? ${isChiled}
// isSenior? ${isSenior}
// `);

// age = 6;
// isChiled = age <= 7;
// isSenior = age >= 65;
// console.log(`
// age = ${age}
// isChiled? ${isChiled}
// isSenior? ${isSenior}
// `);

// age = 69;
// isChiled = age <= 7;
// isSenior = age >= 65;
// console.log(`
// age = ${age}
// isChiled? ${isChiled}
// isSenior? ${isSenior}
// `);
