// You can't define virable more than once
let petName = "mal";
petName = "spot";
console.log(petName);

// There are rules related to variable names
let test_$ = 2;
let _$result = 3;

// Variable names can't br reserved keywords
